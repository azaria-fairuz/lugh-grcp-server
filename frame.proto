syntax = "proto3";

package frameservice;

message Frame {
    string camera_id = 1;
    int32 width = 2;
    int32 height = 3;
    bytes data = 4;
}

message Empty {}

service FrameService {
    rpc SendFrame(Frame) returns (Empty);
}